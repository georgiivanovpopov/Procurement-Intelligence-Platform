services:
  - type: web
    name: tenderlens
    runtime: docker
    plan: standard
    healthCheckPath: /health/ready
    autoDeploy: true
    # Before deploying accounts, attach a persistent disk and set:
    # AccountDbPath=/var/data/accounts.db
    # AccountStorageDurable=true
