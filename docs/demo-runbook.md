# TenderLens demo runbook

1. Confirm `/health/ready` returns `200` after Render wakes.
2. Open `/supplier/175074752`, inspect all five Signal Cards, open Buyer concentration, sort evidence, and open `CAIS:CT-2025-001`.
3. Return with Browser Back and confirm context is preserved.
4. Open `/supplier/000000019` and confirm sparse metrics and all signals say `Insufficient data` rather than zero.
5. Confirm Data Coverage names the fixture snapshot. Replace it with a checksum-bound CAIS manifest before presenting results as real-world procurement analysis.
