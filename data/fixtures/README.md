# Deterministic review fixtures

The ingestion CLI publishes two suppliers: `175074752` exercises the full evidence journey and `000000019` exercises honest sparse-data states. The records are synthetic public-procurement-shaped fixtures and are visibly identified as such in snapshot metadata; they are not claims about real companies.
